If rebase to master, the file will still in rebase_branch, but rebase_branch will add file from master.

Now, in rebase_branch, the file in master can be seen in rebase_branch.
The rebase is success.

